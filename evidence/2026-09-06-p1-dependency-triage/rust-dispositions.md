# Rust warning dispositions

**Current status, 2026-09-06 17:04 UTC:** hosted Ubuntu 24.04 Rust and Windows jobs passed for `a5c48aa`. The GLib exception fingerprint remains stale; renewal is not justified without the toolchain-change evidence required by `security/README.md`. Earlier local failures below are historical, not the current hosted regression result. See [hosted results and renewal review](#hosted-results-and-renewal-review--2026-09-06).

Reviewed 2026-09-06. Owner role: dependency/security maintainer. Review deadline for every record: **2026-10-06**. Reopen every record on crate/parent/feature/target changes, new vulnerability advisory, supported upstream replacement, or expiry. No CVSS/severity supplied for any of these 17 informational warnings; maintenance status is not an exploit rating.

RUNTIME: cargo-audit 0.22.2, database `5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5`, reports zero vulnerabilities, 16 unmaintained warnings and one unsound warning. `rust-audit.json` is the complete unfiltered scanner JSON (no credentials). Count equals historical 17; original historical raw data unavailable, so removed/new IDs cannot be inferred. No notice/yanked entries reported; stderr empty on successful retry. First scan's zero exit with a registry timeout is explicitly rejected as incomplete coverage.

## Target and dependency evidence

RUNTIME: 34 inverse feature trees captured using `cargo tree --locked --manifest-path apps/desktop/src-tauri/Cargo.toml --all-features --target <triple> -i <crate> -e features`. Targets: `x86_64-pc-windows-msvc` (shipping) and `x86_64-unknown-linux-gnu` (development/CI). GLib/GTK/macro warning crates are absent from the Windows graph; Unicode crates are present in both. Also inspected `-e normal,build` for GLib and Unicode chains. The graph includes `desktop-runtime` and `tauri-ipc-test`, not just headless defaults. Linux runtime presence is not silently discarded because Windows ships.

Chains below run parent → warning crate. App → Tauri 2.11.5 is common. Tauri-runtime-wry 2.11.4 uses Wry 0.55.1 and Tao 0.35.3. Tauri uses Muda 0.19.3; Wry/Tauri use WebKit2GTK 2.0.2. These depend on GTK 0.18.2. Exact feature trees are saved locally under `.cache/p1-security/tree-<triple>-<crate>.txt`; commands above reproduce them.

| ID                | Crate/version            | Parent chain / role                                                  | Windows / Linux presence          | API/source-to-sink / disposition                                                                                           |
| ----------------- | ------------------------ | -------------------------------------------------------------------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| RUSTSEC-2024-0413 | atk 0.18.2               | GTK → atk, GUI runtime                                               | absent / runtime                  | No affected function specified: maintenance-only deferral                                                                  |
| RUSTSEC-2024-0416 | atk-sys 0.18.2           | GTK/atk → atk-sys, FFI runtime                                       | absent / runtime                  | No affected function specified: maintenance-only deferral                                                                  |
| RUSTSEC-2024-0412 | gdk 0.18.2               | GTK/WebKit2GTK/gdkx11 → gdk, GUI runtime                             | absent / runtime                  | No affected function specified: maintenance-only deferral                                                                  |
| RUSTSEC-2024-0418 | gdk-sys 0.18.2           | gdk/gtk-sys/gdkwayland-sys/gdkx11-sys → gdk-sys                      | absent / runtime                  | No affected function specified: maintenance-only deferral                                                                  |
| RUSTSEC-2024-0411 | gdkwayland-sys 0.18.2    | Tao → gdkwayland-sys, Wayland FFI                                    | absent / runtime                  | No affected function specified: maintenance-only deferral                                                                  |
| RUSTSEC-2024-0417 | gdkx11 0.18.2            | Wry → gdkx11, X11 runtime                                            | absent / runtime                  | No affected function specified: maintenance-only deferral                                                                  |
| RUSTSEC-2024-0414 | gdkx11-sys 0.18.2        | Tao/gdkx11 → gdkx11-sys, X11 FFI                                     | absent / runtime                  | No affected function specified: maintenance-only deferral                                                                  |
| RUSTSEC-2024-0415 | gtk 0.18.2               | Tauri/runtime/Wry/Tao/Muda/WebKit2GTK → GTK                          | absent / runtime                  | No affected function specified: maintenance-only deferral                                                                  |
| RUSTSEC-2024-0420 | gtk-sys 0.18.2           | GTK/WebKit2GTK sys → gtk-sys, GUI FFI                                | absent / runtime                  | No affected function specified: maintenance-only deferral                                                                  |
| RUSTSEC-2024-0419 | gtk3-macros 0.18.2       | GTK → gtk3-macros, host procedural macro                             | absent / host build for Linux     | Repository/derived macro inputs; no affected function specified: maintenance-only deferral                                 |
| RUSTSEC-2024-0370 | proc-macro-error 1.0.4   | gtk3-macros/glib-macros → proc-macro-error                           | absent / host build for Linux     | Repository/derived macro inputs; no affected function specified: maintenance-only deferral                                 |
| RUSTSEC-2025-0081 | unic-char-property 0.9.0 | Tauri-utils → urlpattern 0.3.0 → unic-ucd-ident → unic-char-property | runtime + build / runtime + build | URL pattern character classification; no affected function specified: maintenance-only deferral                            |
| RUSTSEC-2025-0075 | unic-char-range 0.9.0    | unic-ucd-ident/unic-char-property → unic-char-range                  | runtime + build / runtime + build | URL pattern character ranges; no affected function specified: maintenance-only deferral                                    |
| RUSTSEC-2025-0080 | unic-common 0.9.0        | unic-ucd-version → unic-common                                       | runtime + build / runtime + build | Unicode metadata, no affected function specified: maintenance-only deferral                                                |
| RUSTSEC-2025-0100 | unic-ucd-ident 0.9.0     | Tauri-utils → urlpattern → unic-ucd-ident                            | runtime + build / runtime + build | URL pattern identifier classification; no affected function specified: maintenance-only deferral                           |
| RUSTSEC-2025-0098 | unic-ucd-version 0.9.0   | unic-ucd-ident → unic-ucd-version                                    | runtime + build / runtime + build | Unicode metadata, no affected function specified: maintenance-only deferral                                                |
| RUSTSEC-2024-0429 | glib 0.18.5              | GTK/GIO/GDK/WebKit2GTK/Soup3 and other bindings → GLib               | absent / runtime                  | VariantStrIter iteration unsoundness; bounded **evidenced-unreachable** function deferral (see source/link evidence below) |

Maintenance deferrals acknowledge real runtime/build presence, not unreachable libraries. Each is limited to the exact warning identity, crate/version and these two target scopes. The maintenance advisories do not identify an exploitable sink; user input may reach GUI and URL-pattern APIs. No compatibility-safe replacement was resolved. The Linux maintenance exceptions must never swallow GLib's distinct unsoundness.

## Enforced review context (2026-09-06)

CODE/RUNTIME: the initial fingerprint adoption compared `apps`, `packages`, `pnpm-lock.yaml` and `package.json` against evidence commit `7637138`; no differences were present. The locked manifests, application consumers and generator inputs therefore retain the reviewed construction analysis below. This is a deliberate binding of that existing review, not a new Linux runtime or linked-binary pass. The exception now records `reviewContext` version 1, both `x86_64-pc-windows-msvc` and `x86_64-unknown-linux-gnu`, and a SHA-256 over the conservative repository input inventory, target scope and this evidence path/content. CI explicitly supplies the same two-target inventory and its build definitions are included in the hash.

Any changed/missing inventory input or evidence now fails with `review-required` before GLib can be approved, even when advisory/crate/version/kind remain identical. The boundary includes source-only additions/edits, Cargo manifests/features/lockfile, generator inputs and checked-in generated files. Ignored machine build outputs are not portable review inputs; the generated-code/link observations below remain bounded historical evidence and must be renewed for changed toolchains or external generation. See `security/README.md` for the exact algorithm and deliberate renewal procedure. Scanning cannot refresh approval. Maintenance deferrals, all visible findings and the 2026-10-06 deadline are unchanged.

## GLib function evidence and compatibility ceiling

CODE: exact Cargo registry source `glib-0.18.5/src/variant.rs:843–854` constructs `VariantStrIter` from `Variant::array_iter_str`. Iterator fields are private and its constructor is crate-private (`variant_iter.rs:102–116`). `impl_get` passes immutable `&p` as a C out-argument then calls `CStr::from_ptr(p)` (`:118–130`). `next`, `nth`, `last`, `next_back`, `nth_back` call it (`:138–195`). Direct `count`/`size_hint` do not. Advisory: https://rustsec.org/advisories/RUSTSEC-2024-0429.html (unsound informational; affected >=0.15,<0.20; fixed >=0.20).

RUNTIME/CODE: locked Linux all-feature metadata covers 323 packages; scanned 8,902 Rust source files including locked GTK/WebKit/GLib/Tauri/Wry/Tao/Muda/GIO and application. Producer/type references occur only in GLib's own implementation, re-export, docs and tests. Scoped framework review agrees; Tao's portal Variant comes from DBus, not GLib. This initially left reachability unknown and the gate failing. Step 7 obtained additional Linux evidence through the installed Ubuntu-22.04 WSL environment: locked all-feature/all-target Clippy completed successfully with Rust 1.97.1; the 14 generated Rust files beneath the target build output contain no producer/type references. The linked, unstripped x86_64 all-feature test ELF (`supa_video_desktop_lib-d104cbe8c51bde52`, build ID `c89a796d736e1cb109b4422b7d20d38fcd8308c5`) has no `VariantStrIter`/`array_iter_str` matches across 137,038 `nm -C` lines. Symbol absence alone would not exclude inlining; it corroborates the graph-wide source and generated-file evidence, rather than replacing it.

CODE: Tauri 2.11.5 requires GTK 0.18; GTK 0.18.2 requires GLib 0.18, excluding patched >=0.20. RUNTIME: `cargo update --manifest-path apps/desktop/src-tauri/Cargo.toml -p glib --dry-run`, equivalent targeted Tauri/runtime-Wry/Wry/GTK updates and all maintenance crates/URLPattern/tauri-utils resolve zero updates. No Cargo lock/manifest change made. Do not force GLib 0.20 across GTK's ABI/types, fork locally, disable Linux features, or classify this as maintenance-only.

DEDUCED disposition, reviewed after the additional evidence: the sole public producer is unused in the exact reviewed Linux graph; the Rust iterator is not constructed merely by GTK's C Variant calls. Windows excludes the crate. A narrow **evidenced-unreachable** deferral is justified until 2026-10-06, not a maintenance exception or a claim that GLib is fixed. Independent read-only re-verification found no concrete remaining source-to-construction gap for this bounded conclusion. Reopen immediately for any lockfile, features, target, generated-code or consumer change; this test ELF does not certify every future release or feature subset. The scanner continues to report the advisory unfiltered.

Historical RUNTIME verification limits, before the hosted results below: Windows all-feature locked tests and all-target/all-feature Clippy pass. Linux Clippy passes. Linux Cargo test attempts timed out; the compiled test executable subsequently ran but exposed unattributed failures before its 180-second deadline, including the durable acknowledgement budget, cancelled NeMo request and prepared-publication race tests. These tests were not edited, skipped or weakened. No full Linux runtime-test pass is claimed. Those failures do not provide evidence for iterator safety; the deferral rests on the separate source/construction/link evidence. Phase verification remains incomplete until the regression failures are resolved or separately dispositioned.

## Historical scheduler verification follow-up — 2026-09-06

**Historical decision before hosted verification: VERIFY-BEFORE-SHIP; task acceptance was not met.** No scheduler code, assertions, timeout budgets, Cargo manifests or lockfiles were changed. A timeout panic identifies the failed observation, not its underlying cause; neither a P1-introduced regression nor an environment-only/historical scheduler defect is established.

### Revisions and environment

- Baseline: `665cf62d92512c70621c0e94f71f0ff1d84787d7`; scoped: `12da4dbefacacf1b2502b811514a8cf10b7d6f47`. `git diff --exit-code` across `apps/desktop/src-tauri` returned zero. Separate `git archive` extractions exist at `/tmp/supa-p1-verify-voCEZ8/{baseline,scoped}` in WSL. No checkout/reset or working-tree rewrite occurred. Cargo generated `gen` only in the scoped copy.
- Available Linux: Ubuntu 22.04.5, WSL2 kernel `6.18.33.2-microsoft-standard-WSL2`, eight processors, ext4 `/tmp`; GTK 3.24.33, WebKitGTK 2.50.2. Rust explicitly selected through `RUSTUP_TOOLCHAIN=1.97.1`: rustc `8bab26f4f`, Cargo `c980f4866`, LLVM 22.1.6; native target `x86_64-unknown-linux-gnu`. The default stable toolchain here is 1.92.0 and was NOT used for these Cargo attempts.
- Cargo attempts used `--locked --all-features` (including `desktop-runtime` and `tauri-ipc-test`), `RUST_BACKTRACE=full`, `CARGO_NET_OFFLINE=true`, and the existing `/home/reaver47/.cache/supa-video-p1-target` build cache. No explicit target, RUSTFLAGS or TMPDIR override was applied.

### Observations, not a baseline comparison

The requested exact Cargo commands were attempted in the scoped source copy. The initial two-command wrapper was aborted by the tool after 300 seconds; its final aggregate output/exit status was not captured. Its per-test logs contain compilation output, not completed tests. Subsequent priority attempts recorded exit **124** at 220 and 270 seconds, still compiling Tauri/application code. Those are build-attempt deadlines, not scheduler test results. No fresh baseline test executable was built; the required same-environment revision comparison remains incomplete. Repeating short compilation deadlines is not a useful next step.

To recover the missing panic summaries without another build, each exact test was then invoked separately on the previously recorded all-feature ELF, with `--exact --nocapture` and full backtraces. Its build ID still matches the earlier evidence: `c89a796d736e1cb109b4422b7d20d38fcd8308c5`; SHA-256 `264b59c74f71ce6e388b01220081374b0397e1e261884a705d344a527075fd5f`. **This cached executable is diagnostic evidence only, not a fresh build of either revision.**

| Exact scheduler test suffix                                            | Completed result                                   | Panic                                                |
| ---------------------------------------------------------------------- | -------------------------------------------------- | ---------------------------------------------------- |
| `priority_fifo_aging_and_bounded_ffmpeg_concurrency_are_deterministic` | Exit 101; 0 passed, 1 failed, 290 filtered; 14.83s | `scheduler.rs:1195:14`, unwrap of `Err(Elapsed(()))` |
| `transient_failure_retries_at_injected_delay_and_completes_once`       | Exit 101; 0 passed, 1 failed, 290 filtered; 6.48s  | `scheduler.rs:1438:14`, unwrap of `Err(Elapsed(()))` |

Both cached tests fail alone, so an isolated-pass-to-concurrency search was not warranted. A retry-only `strace -f -c` rerun also exited 101 at the same assertion; 39 fsync calls totaled about 5.4ms. This does not confirm storage delays or missed wakeups as the cause. No Tokio implementation assumptions or scheduler edits were made.

Local raw logs, full panic output, completed exit files and environment fingerprint: `.cache/p1-security/scheduler-verify-20260906/`. Files prefixed `existing-` refer only to the cached ELF. Files prefixed `scoped-` refer to incomplete Cargo builds. No Cargo/test processes from these attempts remained at the final process inspection.

### Hosted gate and exact remaining verification

Read-only GitHub inspection found the configured Ubuntu 24.04 Rust job **failed at formatting**, with Clippy, all-feature tests and release-budget tests skipped, on both revisions:

- [Scoped run 34042241316, Rust job 101511004976](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34042241316/job/101511004976).
- [Baseline run 34021054130, Rust job 101453591010](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34021054130/job/101453591010).

This confirms an existing formatting-gate failure, not a scheduler regression classification. No CI rerun, push or deployment was triggered. Ubuntu 24.04 is not installed locally; Docker's Linux daemon is unavailable. At the user's subsequent direction, local Linux verification is stopped: do not install/start WSL, Ubuntu or Docker, or create further local build copies. Continue verification only through the existing Ubuntu 24.04 GitHub Actions Rust job.

Final hosted inspection at 15:53 UTC confirmed current HEAD `12da4dbefacacf1b2502b811514a8cf10b7d6f47` already has completed CI run `34042241316`, conclusion **failure**. Its Rust job failed formatting; Clippy, canonical-engine/recovery tests and release-budget tests were skipped. No push or dispatch is needed to obtain a run for this commit; rerunning unchanged source does not resolve its formatting failure. No Linux pass is claimed, and the verification task remains open.

Remaining at that historical checkpoint, using hosted Ubuntu 24.04 only: build both revisions under the same recorded environment and run each of the two exact commands from the verification request with full output and exit status. If they both pass alone, minimize the failing concurrent combination and compare both revisions. Then obtain the complete configured Ubuntu 24.04 Rust gate: formatting check, locked all-target/all-feature Clippy with `-D warnings`, unfiltered locked all-feature tests, and both configured release-mode storage-budget tests. Fix only a confirmed scoped defect at its shared cause, retaining original assertions. Existing formatting failure must be resolved separately before hosted CI reaches those tests. A WSL diagnostic failure/pass or source-tree equality cannot substitute for that green gate; Windows results cannot establish a hosted Linux pass.

## Hosted results and renewal review — 2026-09-06

### Observed hosted results

RUNTIME (hosted logs/API, not local Linux): [run 34045878915](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34045878915) tested `a5c48aa1ccd7912e203b4eaae1f1545ea6a99a0c`.

- [Ubuntu 24.04 Rust job 101520712888](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34045878915/job/101520712888) passed formatting, locked all-target/all-feature Clippy, the full locked all-feature test command, and configured release-mode storage budgets. Main unit binary: 280 passed, zero failed, 12 existing ignored; integration binary: five passed. Both previously flagged scheduler tests, the original bounded/redacted profile-lock test, and the new duplicate-handle release regression passed. No assertions, timeout budgets or ignore annotations were relaxed by these fixes.
- [Windows job 101520712991](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34045878915/job/101520712991) completed successfully at 17:00:10 UTC. Clippy, native tests, repeated lock/crash-recovery proof, packaging, startup smoke and packaged media IPC all passed. Packaged IPC took 8m22s, ending at 17:00:01 UTC; it was not a remaining hang.
- [Security job 101520712758](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34045878915/job/101520712758) failed the Rust exception evaluation: `review-required: reviewed inputs changed for ["RUSTSEC-2024-0429","glib","0.18.5","unsound"]`. Its actual Rust scanner exit was 1, not zero. The exact maintenance findings remained visible; a passing regression suite is not permission to ignore the stale GLib review.
- The separate TypeScript job failed formatting of one array assertion in the dependency-policy tests. Local commit `012f381` fixes that formatting only; this commit was not tested by run 34045878915. These results do not establish a green entire workflow or release readiness.

The earlier WSL scheduler failures were not attributed by a same-environment baseline comparison. The retained-handle profile-lock release defect was reproduced on Windows before the fix and its regression passed afterward, including in the hosted Linux suite. That does not prove the cause of every prior WSL failure. No local Linux, installation, new build copy or hosted rerun was used for this renewal review.

### Changed fingerprint inputs and exception scope

CODE: reviewed all six changed tracked input paths between fingerprint-adoption commit `3483a2c` and `012f381`, plus the previously uncommitted changes to this evidence file:

| Input                                            | Review conclusion                                                                                                                                                       |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/desktop/src-tauri/src/video/cache.rs`      | Rust formatting only; no changed behavior or assertions.                                                                                                                |
| `apps/desktop/src-tauri/src/video/derived.rs`    | Explicit fs4 unlock on guard drop and a retained-handle regression; only file locking and test setup change, with no GLib Variant construction or new dependency.       |
| `scripts/check-dependency-policy.mjs`            | Public validator now rejects unsupported yanked exceptions; no broader advisory approval or fingerprint bypass.                                                         |
| `scripts/tests/check-dependency-policy.test.mjs` | Coverage for rejecting yanked exceptions and retaining scoped notice approvals, plus array formatting; assertions remain fail-closed.                                   |
| `scripts/tests/local-security-recipes.test.mjs`  | New isolated command-recipe tests cover checksum rejection, actual scanner status propagation and independent gate failures; not application code or a build generator. |
| `security/README.md`                             | Local recipes now preserve scanner statuses and fail closed; yanked exception prohibition documented. Fingerprint renewal requirements unchanged.                       |
| This evidence file                               | Records historical investigation and current hosted results; changing its content also invalidates the fingerprint.                                                     |

Cargo manifests, Cargo.lock, dependency versions, feature declarations, target definitions, CI workflow, application generators and checked-in generated code are unchanged in that comparison. Thus the source changes do not introduce a new path to `Variant::array_iter_str` / `VariantStrIter`, and do not alter the recorded Windows absence or Linux dependency presence. Neither target is dropped: `x86_64-pc-windows-msvc` and `x86_64-unknown-linux-gnu` remain the complete declared scope. All 16 maintenance-only deferrals and their 2026-10-06 expiry remain unchanged; GLib's distinct unsound exception is not reclassified as maintenance-only.

### Historical checkpoint: why the GLib fingerprint was not renewed

RUNTIME/CODE: hosted Rust logs identify **rustc 1.98.1 (`48a229cea`, 2026-09-01)**, whereas the recorded generated-source and linked-ELF review above used Rust 1.97.1 on Ubuntu 22.04. `security/README.md` explicitly requires renewal of generated-code/link evidence on toolchain or external-generation changes. The artifact inventory for run 34045878915 contains only `supa-video-producer-startup-34045878915`; it does not publish the Linux generated Rust files or unstripped ELF needed to repeat that review. Passing Clippy/tests does not inspect construction of the advisory's affected iterator, and cannot substitute for that evidence.

DEDUCED: source-delta review supports retaining the bounded construction analysis as historical evidence, but does not establish the required current generated-code/link corroboration. No `reviewContext.sha256`, exception identity, target, rationale, owner or deadline is changed. The gate must continue to fail `review-required`; printing/copying a new digest would hide this unresolved review requirement. The smallest remaining verification is a hosted-only capture and review of the current Linux generated Rust sources and unstripped test ELF, with its toolchain/target/features recorded, checking both `VariantStrIter` and `array_iter_str` references. No new reference would still need to be interpreted alongside the source-construction analysis, not treated as proof from symbol absence alone. Adding that capture or triggering CI is not performed in this task.

RUNTIME: a fresh network-enabled local Windows `cargo-audit 0.22.2 --file apps/desktop/src-tauri/Cargo.lock --json --deny warnings` returned **1**, preserved verbatim in `.cache/p1-security/renewal-20260906/rust.exit`; JSON and stderr are separate adjacent files. Database commit `5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5`; zero reported vulnerabilities, 16 unmaintained and one unsound warning, empty stderr. This is a fresh lockfile audit, not local Linux execution or a clean raw scanner exit. No notice/yanked findings were reported. Policy evaluation must use this actual exit and both CI target triples. No successful policy check or fingerprint renewal is claimed.

## Authorized GLib review closure — 2026-09-06

The preceding uncommitted investigation and disposition work is retained as historical evidence. The user separately authorized this closure after artifact verification and read-only delta review. This section supersedes the earlier missing-artifact obligation, not its historical observations or limits.

### Exact reviewed input delta

CODE: the recorded review digest exactly matches fingerprint-adoption commit `3483a2c` (419 fingerprint inputs, excluding the exception document). The complete tracked delta from that context to `3625fccbbc49c854a20b957d8a3f69d1cc6da8f3` comprises these nine paths; line references are at `3625fcc`:

| Reviewed input                                                   | Change and effect on the construction argument                                                                                                                                   |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/desktop/src-tauri/src/video/cache.rs:751–761,1669–1784`    | Formatting only; no changed consumer, behavior or assertion.                                                                                                                     |
| `apps/desktop/src-tauri/src/video/derived.rs:168–211,2692`       | Explicit fs4 file unlock on guard drop and duplicate-handle regression; no GLib Variant construction or dependency change.                                                       |
| `scripts/check-dependency-policy.mjs:165–168`                    | Rejects unsupported yanked exceptions; no reachability change or fingerprint bypass.                                                                                             |
| `scripts/tests/check-dependency-policy.test.mjs:149–174,316–322` | Yanked/notice policy coverage and assertion formatting; no application or generator change.                                                                                      |
| `scripts/tests/local-security-recipes.test.mjs:1–143`            | Isolated scanner-recipe tests; not application code or a Rust generator.                                                                                                         |
| `security/README.md:11–126`                                      | Fail-closed recipes and evidence-capture documentation; no application reachability change.                                                                                      |
| `.github/workflows/ci.yml:173–237`                               | Pins Rust 1.98.1, disables target cache, records successful all-feature test output and uploads evidence. Changed toolchain/generation requires the current corroboration below. |
| `scripts/collect-glib-evidence.mjs:1–605`                        | New evidence collector, including explicit Node imports in `3625fcc`; selects and inspects existing debug outputs, not an application-code generator.                            |
| `scripts/tests/collect-glib-evidence.test.mjs:1–368`             | Collector fixture tests; no application reachability change.                                                                                                                     |

Cargo manifests, Cargo.lock, dependency versions/features, application build scripts and checked-in generator inputs remain unchanged. The retained uncommitted changes to this disposition document are an additional local fingerprint input, not part of the hosted `3625fcc` checkout. This closure adds only review documentation and the separately excluded GLib digest; it changes no executable build input.

### Verified hosted artifact and results

RUNTIME (hosted receipts and Windows archive inspection, not local Linux execution): [run 34063337893, attempt 1](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34063337893/attempts/1) used the push event, workflow and actual checkout `3625fccbbc49c854a20b957d8a3f69d1cc6da8f3`, tree `7871add1228d7741b3022a85f3ba7af2df69b00f`, on `review/hosted-glib-evidence-20260906`. All 423 tracked artifact input hashes match that commit. Before/after tracked state is clean. Runner: Ubuntu 24.04 image `20260831.293.1`, X64; Rust 1.98.1 (`48a229cea`), host-native `x86_64-unknown-linux-gnu` without `--target`.

[Artifact 9998412816](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34063337893/artifacts/9998412816) is retained locally as `.cache/p1-security/retrieved-34063337893/outer.zip`. The outer ZIP SHA-256 matches GitHub metadata: `702f57d0b198c35c0b91364c717d65c565eb32391c407087037c350b60ddb8d5`. The inner `hosted-glib.tar` matches its sidecar: `007ea029ec3c0ff67c3fe12136f48103a6f8061f70c9888601e14b434d78eb03`. Both archive layers and all 24 manifest payload entries were checked for expected paths, regular types, sizes and SHA-256; no missing/additional payload or manifest entry was found. Inspection read archive bytes without executing the ELF.

The selected `supa_video_desktop_lib-1d7804702dca7cd2` is the matching unstripped **debug library test ELF**, GNU build ID `5162d425fffc6da23eb0369f7b09fc608047d608`. Cargo records `fresh: false`, `test: true`, opt-level `0`, debuginfo `2`, debug assertions and overflow checks enabled; features are exactly `default`, `desktop-runtime`, `tauri-ipc-test`. The recorded locked all-feature invocation, successful build-finished message and Cargo/tee exits `0/0` agree. ELF inspection commands succeeded; `.symtab` and `.debug_info` are present.

All 14 generated Rust files were reconciled with build-script output mappings and the independent debug-source inventory; none contains `VariantStrIter` or `array_iter_str`. The full `nm-demangled.txt:1–135552` likewise contains neither name. Source categories comprise SQLite bindings, selectors, serde/serde_core, target-lexicon, thiserror, web_atoms and x11-dl. Same-job generation includes Clippy outputs reused by tests; this is not a claim that each output was generated in the test step. No unmaterialized proc-macro expansion coverage is claimed.

The [Linux Rust job 101567681194](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34063337893/job/101567681194) passed formatting, Clippy, all-feature tests, release-budget tests and capture. [TypeScript job 101567681054](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34063337893/job/101567681054) and [Windows job 101567681159](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34063337893/job/101567681159) passed. [Security job 101567681168](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34063337893/job/101567681168) failed only Rust policy with `review-required: reviewed inputs changed for ["RUSTSEC-2024-0429","glib","0.18.5","unsound"]`; the secrets scan passed. Signed installers were skipped. That historical policy failure is not a lint/build failure or a green entire workflow.

### Two-target conclusion and retained limits

CODE/DEDUCED: the unchanged sole public producer and private-constructor analysis (`glib-0.18.5/src/variant.rs:843–854`, `variant_iter.rs:102–195`) remains supported by the graph-wide source review and the consumer delta above. The current generated files and linked symbols supply the previously missing Rust 1.98.1 corroboration. Symbol absence alone cannot exclude inlining and does not replace the construction argument.

Windows absence is separately source-deduced, not inferred from Linux output or the historical empty Windows tree file (which lacks a captured exit receipt). All 469 locked manifests were available for the read-only reconciliation, including renamed dependencies and normal/build/dev edges. The incoming GLib ancestor paths are gated off Windows: cached `Cargo.toml` references `tauri-2.11.5:335–343`, `tauri-runtime-2.11.3:99–106`, `tauri-runtime-wry-2.11.4:106–116`, `tao-0.35.3:271`, `wry-0.55.1:206–229`, `muda-0.19.3:93` and `tray-icon-0.24.1:95`. Optional features retain those target predicates. Dialog's RFD GTK feature (`tauri-plugin-dialog-2.7.2:56–63,104–107`) remains Linux/BSD-gated by `rfd-0.16.0:94–105`. No build-dependency edge enters the GLib ancestor closure; dependency-only dev tests are not activated by the application test invocation.

DEDUCED verdict: the existing narrow evidenced-unreachable function rationale remains supported for the recorded two-target scope. This closes the reviewed delta and missing current debug-source/link obligation, not GLib's advisory or library-wide safety. Debug-test evidence is not a production/release executable, proof for every feature subset, or a signed reproducible-build attestation. Passing release-budget tests does not turn this ELF into release evidence. Reopen on lock, feature, target, consumer, toolchain or external-generation changes.

Only the GLib `reviewContext.sha256` is authorized to change after this document's final formatting, using the documented `reviewFingerprint` helper. Advisory/crate/version/kind, disposition, rationale, both target lists, review-context version, owner and `2026-10-06` expiry remain unchanged, as do all maintenance deferrals. The historical statements that no renewal occurred apply to their earlier checkpoints. Fresh local checks and the single subsequent hosted validation are reported separately; no future result is presumed here.

## 2026-09-07 range-validation delta and authorized fingerprint renewal

This addendum preserves every historical checkpoint above. Reviewed checkout: `3810297b21d22d45c3f573e9482c5605cd21a2a2`, tree `4d074fa98d9700ac2c6006cbec76f8a862dd9271`, on `wip/project-v2-range-validation-20260907-061706`; Git resolved the SHA and the worktree was clean before this documentation change. Relative to `580aabf79d3fbea20a705159ae7170b5669150a0`, the complete delta is five files, +484/-0:

- `apps/desktop/src-tauri/src/video/project/integrity.rs`: +2, require identical endpoint rate numerators and denominators before comparing range values.
- `apps/desktop/src-tauri/src/video/project/tests.rs`: +83, reject a rehashed journal with mismatched endpoint rates without repairing files or creating a session.
- `packages/video-contracts/src/project-v2.ts`: +7, matching common-rate refinement and explicit half-open range documentation.
- `packages/video-contracts/src/project-v2.test.ts`: +68, endpoint-rate, empty/reversed, fractional-rate and safe-integer boundary checks, including history and command-result consumers.
- `packages/video-contracts/fixtures/project-v2/manifest.json`: +324, shared range fixtures; the final commit adds empty `[30,30)`, reversed `[31,30)`, and valid `[9007199254740990,9007199254740991)` cases at 30 fps (+108 of this total).

CODE/DEDUCED: these changes tighten scalar range validation and add tests; they add no GLib producer, iterator construction, dependency, feature, generator, target or workflow change. The existing nonempty ordering rule remains. No production code is changed by this renewal.

### Exact hosted receipts and inventory verification

RUNTIME: [run 34097241777, attempt 1](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34097241777/attempts/1), push event in `creativeprofit22/supa-video-produzah`, matches the checkout/event/workflow SHA above and the WIP branch ref. Linux runner image is `ubuntu24` / `20260831.293.1`; compiler is Rust 1.98.1, commit prefix `48a229cea`, target `x86_64-unknown-linux-gnu`. Before/after checkout and tree agree, tracked status is clean, and all 423 tracked-input SHA-256 values match Git blobs at the exact reviewed SHA. The target was initially empty.

[Artifact 10009540393](https://github.com/creativeprofit22/supa-video-produzah/actions/runs/34097241777/artifacts/10009540393), named `hosted-glib-3810297b21d22d45c3f573e9482c5605cd21a2a2-34097241777-1`, was independently downloaded and verified against GitHub metadata. Verified SHA-256 receipts:

- Outer ZIP: `84ab850a1894e8872904e03eebbd66b4d3ffa795f6fd000601e6024c07dc033d`.
- Inner `hosted-glib.tar`, matching its sidecar: `3c269eef3d32b3cc6a2151eb8e584b5948421becfe7439ed91fc713916b85433`.
- `cargo-metadata.json`: `185dfdeed1f4975621df4cd71c737d290f368313dd03abe940e48b0fca654b94`.
- `generated-sources.json`: `19adc7ade5f18aad4ec0736c8e5276441c372eff6aba35c3c418afd9d5679c19`.
- `provenance.json`: `16331da718b556c04d7d96859df3ba5248b524988e2d7991e1dd7a10946924bc`.
- `elf/supa_video_desktop_lib.test.elf`: `2842f215e58a01e78e0ba9f0954880253cf8b649fcebbc7fd09e097645f6a64f` (281521712 bytes).

Both archive layers were checked before extraction for exact entries, normalized paths, duplicate names, directory inventory and regular file types. All 24 manifest payload entries match their sizes and hashes, with no missing/additional files; the tar digest protects the manifest itself. No ELF was executed. All 67 build-script messages match unique directory mappings, including directories without Rust outputs. All 14 generated Rust files match the independent file inventory and hashes: 11 reported-build-script files and three labeled same-job debug extras; zero repository-generated untracked Rust files. Explicit UTF-8 decoding passed after an initial Windows default-codepage decoding error; this was a verifier error, not an artifact mismatch.

The Cargo-selected `supa_video_desktop_lib-1d7804702dca7cd2` is an unstripped debug library test ELF with GNU build ID `fc7d3ee6491002efc314f075abe49ca2ea46095d`, `.symtab` and `.debug_info`. Recorded features are exactly `default`, `desktop-runtime`, `tauri-ipc-test`; profile is test/debug, opt-level 0, debuginfo 2, debug assertions and overflow checks enabled, `fresh: false`. Cargo/tee exits are 0/0; inspection commands succeeded. Hosted tests report 281 passed and 12 ignored, plus five passing integration tests; zero failures.

### Construction argument, Windows scope and limits

CODE/DEDUCED: GLib 0.18.5 `VariantStrIter` has private fields and a crate-private constructor (`variant_iter.rs:102–115`); `Variant::array_iter_str` is its sole public producer (`variant.rs:843–854`). The affected iterator methods require that constructed value. The locked hosted Linux metadata contains 323 packages; renewed local source inspection covers 8902 Rust files at those package versions. Seventeen exact producer/type hits are confined to GLib definitions, imports/exports, documentation and dependency-only tests, not downstream consumers. Four broad substring hits in bitflags were unrelated `to_writer_strict` identifiers, not producer calls. Dependency-only tests are not activated by the application test invocation. The reviewed five-file delta preserves this construction argument.

RUNTIME/CODE: all 14 captured generated files have no producer/type use. The usable full demangled symbol report contains other GLib symbols but neither `array_iter_str` nor `VariantStrIter`. This corroborates the construction/source argument; symbol absence alone cannot exclude inlining and is not proof of unreachability.

RUNTIME/DEDUCED: a fresh successful `cargo metadata --locked --all-features --format-version 1 --filter-platform x86_64-pc-windows-msvc --manifest-path apps/desktop/src-tauri/Cargo.toml` at the exact clean SHA resolves 299 nodes with no `glib`, `glib-sys`, `gtk`, `gtk-sys` or `gobject-sys`. This independently supports the unchanged target-gated manifest analysis above; it is not inferred from Linux symbols or a missing Windows output file.

Limits remain: same-job generation can include Clippy outputs reused by tests, not necessarily same-step generation. Unmaterialized proc-macro expansions are not captured. Registry source inspection is local at the locked versions, not a hosted source archive. This debug test ELF is not a production/release binary, proof for every feature subset, or a signed reproducible-build attestation. Artifact retention is 30 days. These checks support only the existing bounded two-target function deferral, not library-wide safety or resolution of the advisory. Reopen on lock, feature, target, consumer, toolchain or external-generation changes.

### Hosted failures, skipped work and renewal boundary

The Linux Rust job `101663502455` passed formatting, Clippy, all-feature tests, release-budget tests, collection and upload. TypeScript job `101663502426` and Windows job `101663502473` passed. Dependency-policy/full-history-secrets job `101663502410` failed at exact Rust identity/review-context enforcement because the fingerprint was stale; its secrets scan passed. Its Node post-install cleanup step was skipped. Signed Windows installers and clean-runner smoke job `101670008537` was skipped. The entire hosted workflow was not green; ignored tests and skipped packaging are not passing evidence.

The user separately authorized documenting this delta and renewing only the existing GLib `reviewContext.sha256` after formatting final inputs. Preserve advisory, crate/version/kind, disposition/rationale, both target lists, review-context version, owner, expiry `2026-10-06`, maintenance deferrals and all enforcement rules. Historical receipts remain unchanged. Required fresh local collector/policy tests and Rust audit/policy results are reported separately after the fingerprint update; no future local or hosted pass is presumed. This renewal does not authorize production changes, merge or push.

## 2026-09-13 focused renewal attempt — not renewed

The user authorized recording the delta and running focused dependency audits and policy tests, with fingerprint renewal only if supported. Reviewed local HEAD: `b21a303c51c258d3aa63fa51217bcbb4508fef0f`, including the existing dirty Cargo test-profile and cancellation changes. This is not a clean-commit attestation. Existing user changes and the unresolved supervisor-timeout note were left untouched.

CODE/DEDUCED: comparison against renewal commit `eb4ea08` found no lockfile, dependency-feature, target-declaration, build-script, security-policy or CI change. Production deltas concern cancellation/SQLite settlement, checkpoint lifecycle, sanitized cleanup reporting and caption endpoint timing. A scoped search of application Rust source found no `array_iter_str`, `VariantStrIter`, GLib or GTK references; the only searched macro-definition hit was an existing test helper. This supports retaining the bounded source-construction argument, not a new binary-wide assurance. The new `[profile.test.package.sha2] opt-level = 3` changes the test build; the historical Linux ELF and generated-source receipts remain historical. External dependency sources, current generated outputs and current Linux binaries were not regenerated or fully re-reviewed.

### Executed checks

Environment: Node `v22.20.0`, pnpm `10.34.5`, cargo-audit `0.22.2`.

- RUNTIME: `node --test scripts/tests/check-dependency-policy.test.mjs scripts/tests/local-security-recipes.test.mjs scripts/tests/collect-glib-evidence.test.mjs` — **62 passed, zero failed/skipped; exit 0**. Execution receipt: `1438835c-6d14-4f63-835c-d38b9db26ecf`.
- RUNTIME: `pnpm audit --json` — scanner exit **1**, empty stderr, two moderate findings (`1193683` in vitest and `1193684` in @vitest/mocker, both 4.1.10). The existing JavaScript policy accepted both, with `ok: true` and no policy errors. This is an accepted-exception result, not zero advisories.
- RUNTIME: `cargo audit --file apps/desktop/src-tauri/Cargo.lock --json --deny warnings` — scanner exit **1**, empty stderr. Reported zero entries in the vulnerabilities list and **seven warnings**: six unmaintained packages and GLib 0.18.5's unsoundness warning. No ignore entries or architecture/OS filters were recorded in the JSON settings. Report database: `b50980aad8b8f14f77e25a97b32dd94bf008b0af`, 1243 advisories, last updated `2026-09-09T12:49:52+02:00`.
- RUNTIME: Rust policy using the unchanged exception file and both declared targets returned **`ok: false`**. The wrapper preserved the failed gate as exit **1**. In addition to the expected GLib `review-required` fingerprint mismatch, it rejected ten stale exception identities absent from this audit: atk, atk-sys, gdk, gdk-sys, gdkwayland-sys, gdkx11, gdkx11-sys, gtk, gtk-sys and gtk3-macros (the existing RUSTSEC-2024-0411 through -0420 identities). Execution receipt for audits and policies: `3695d14d-66ab-47a4-ae3f-0feb98aea19e`.

The reason those ten warnings are absent is **not established**. Do not interpret absence as upstream withdrawal or resolved maintenance risk. A separate read of `$HOME/.cargo/advisory-db` found HEAD `0bfde9d6a469ae503f8a6147c2dd552856cd5999`, different from the database identified by the scanner, and its gtk advisory still described unmaintained bindings. That separate checkout is not proof of which database cargo-audit used.

### Retained raw-result provenance

Local raw results are in `.cache/p1-security/renewal-20260913-5TCSK5/` (ignored, not committed). SHA-256:

- `rust.json`: `821c359833c5b267aae7b96deb7e847c1b9461a1169b3a6af56f8451fc5f3b2d`
- `javascript.json`: `0d7ca559b86656e66aa3615d9f29ed51524884da5a5958a3ea50fa3dbd1d662d`
- Both empty stderr files: `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`
- Both exit files (`1` plus newline): `4355a46b19d348dc2f57c046f8ef63d4538ebb936000f3c9ee954a27460dd865`

This durable summary does not embed the complete raw logs; cache cleanup can remove them. Historical evidence above is unchanged.

### Decision and remaining scope

**No fingerprint renewal and no exception removal.** The changed warning inventory needs diagnosis against the scanner's actual database before any approval change. All enforcement rules, owners, expiry dates, target lists and dependencies remain unchanged. No application code, Roadmap status, commits or hosted jobs were changed. Full-history secret scanning and the full application test matrix were not rerun; the intermittent Windows supervisor timeout remains unresolved. The test pass above precedes this documentation-only addendum and is not a passing dependency-policy result for the amended review inputs.

### Follow-up diagnosis: ten upstream withdrawals confirmed

The later investigation resolves the warning-inventory uncertainty above without rewriting that historical failed attempt.

RUNTIME/CODE: this shell sets `CARGO_HOME=E:\DevCaches\cargo`. The relevant database is therefore `E:/DevCaches/cargo/advisory-db`, not the older checkout under `$HOME/.cargo`. Its HEAD is `b50980aad8b8f14f77e25a97b32dd94bf008b0af`, matching the original scanner report, and its origin is `https://github.com/RustSec/advisory-db.git`. No database files or Git settings were changed. Both local database checkouts contain an existing untracked nostr advisory; it was left untouched, and the Git-blob checks below do not rely on it.

CODE: upstream commit [`b266fb89baa88c73c6aaa53e0e87509c80bdf962`](https://github.com/RustSec/advisory-db/commit/b266fb89baa88c73c6aaa53e0e87509c80bdf962), titled “Withdraw gtk3-rs unmaintained advisories (#3189)”, adds `withdrawn = "2026-08-14"`. Independent `git show` reads at the exact scanner-reported database commit confirmed this field for **all ten** stale identities: RUSTSEC-2024-0411 through RUSTSEC-2024-0420. The advisory update says the gtk-rs repository was unarchived, README warnings were removed and development resumed. These are withdrawals of maintenance advisories, not dependency upgrades or fixes to GLib's separate unsoundness advisory.

RUNTIME: a controlled rerun explicitly selecting that database, with fetching disabled and the same lockfile/deny settings, again reported the same seven warning IDs and scanner exit **1**:

```sh
cargo audit --db E:/DevCaches/cargo/advisory-db --no-fetch --file apps/desktop/src-tauri/Cargo.lock --json --deny warnings
```

Execution receipt: `f14184be-97bc-427d-bd95-68083a6e255e`. Raw report, stderr and exit are retained alongside the earlier results as `rust-explicit-db.json`, `rust-explicit-db.stderr` and `rust-explicit-db.exit`. The no-fetch report leaves database commit/time null; the matching Git HEAD comes from the separate repository inspection, not those null report fields. Wrapper exit 0 denotes successful capture, **not a passing audit**.

Conclusion: the ten missing warnings are explained by upstream withdrawals in the database cargo-audit actually uses. The policy's exact-inventory rejection is working as designed. The next scoped change is to retire those ten active exceptions while preserving this history, then separately finalize the GLib review fingerprint and rerun the focused policy checks. This diagnostic follow-up changes no exceptions, fingerprint, application code or Roadmap status.

### Authorized policy reconciliation (2026-09-13)

The user subsequently authorized retiring the ten withdrawn-advisory exceptions, finalizing the existing GLib review and rerunning focused policy checks. Only the ten maintenance exceptions RUSTSEC-2024-0411 through RUSTSEC-2024-0420 are retired; their original dispositions and withdrawal evidence remain in this document and Git history. Six maintenance deferrals remain, along with the narrowly scoped GLib unsoundness deferral. No warning is ignored, no enforcement is relaxed, and no dependency changes.

The GLib renewal accepts the source delta reviewed above against `eb4ea08`, including the existing dirty cancellation changes, SHA-256-only test optimization and engineering notes. Dependency locks/features/targets and generation configuration remain unchanged; reviewed application changes do not introduce a consumer of the affected producer. Earlier external-source and Linux binary receipts remain corroborating historical evidence, not freshly generated evidence for this working tree. The renewal does not assert that a current Linux ELF was inspected, that all features/platforms are safe, or that the Windows timeout is fixed. The bounded function deferral is retained on that combined, qualified evidence; its advisory identity, rationale, two-target scope, owner and expiry remain unchanged.

The fingerprint is computed only after the final review-note inputs are saved. Focused tests and policy checks are run separately; their execution receipts are the authority for pass/fail. No Roadmap completion is implied by this policy reconciliation.

### Authorized pre-commit delta review (2026-09-15)

The user explicitly authorized reviewing the changed approval inputs and renewing only the existing bounded approval if supported, preserving enforcement and scope, before atomic commits and push. This review covers the working-tree delta at HEAD `b21a303c51c258d3aa63fa51217bcbb4508fef0f0` and the earlier delta against `eb4ea08`; it is not a clean-checkout or current Linux binary attestation.

CODE: compared with `eb4ea08`, the Cargo lockfile, build script, Cargo configuration, CI workflows and dependency-policy implementation are unchanged. The manifest adds the already pinned Tokio 1.53.1 `test-util` development feature and the previously recorded SHA-256 test optimization. Tokio's installed manifest defines `test-util` as `rt`, `sync` and `time`; these feature definitions add no GLib dependency or producer. Unlike the September 13 review, this review explicitly includes that development-feature change. Application changes concern job cancellation/cache settlement, exact clip speed/fade timing, project commands and persistence, render validation, and editor preview/controls; no GLib/GTK or affected iterator producer/type reference was found in application Rust source. The only searched macro-definition hit remains the existing test helper. The dated copied capture prototypes and generated outputs stay local and outside the Git inventory; curated reports remain tracked. This is publication scoping, not a change to the policy's inventory algorithm.

RUNTIME: fresh `cargo metadata --offline --locked --all-features --format-version 1 --filter-platform <target> --manifest-path apps/desktop/src-tauri/Cargo.toml` succeeded for both declared targets. Windows resolves 299 nodes and no `glib`, `glib-sys`, `gtk`, `gtk-sys` or `gobject-sys`. Linux resolves 323 nodes, retaining GLib 0.18.5 and GTK 0.18.2. Execution receipt: `7ca48164-2e33-4a3e-9e54-084013cf9619`.

RUNTIME/CODE: a renewed local scan of all 323 Linux-resolved package source roots read 8912 Rust files and found 17 exact `array_iter_str` / `VariantStrIter` matches, all confined to GLib definitions, imports/exports, documentation and dependency-only tests. No downstream consumer was found. Receipt: `7c72cddf-c76f-4f79-8b82-f25ed2566c72`. Direct source inspection reconfirmed private iterator fields and the crate-private constructor at `glib-0.18.5/src/variant_iter.rs:102-115`, with `Variant::array_iter_str` constructing the value at `variant.rs:843-854`. Dependency-only tests are not application tests. This retains the existing construction argument rather than inferring safety from a missing symbol.

RUNTIME: `pnpm lint`, `pnpm check` and `pnpm test` passed after narrowing lint exclusions to the safety-disabled historical snapshots and declaring diagnostic-script globals. `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml --locked --offline` passed 333 library tests and five security-configuration integration tests; 12 existing environment-dependent library tests remain ignored. All 62 `scripts/tests/*.test.mjs` tests passed, including policy and hosted collector tests. Rust/Node execution receipt: `32a26b81-8232-4dbd-85a9-cc3774794244`. These are local default-feature test results, not hosted/all-feature or media-capture results.

RUNTIME: the explicit no-fetch audit reproduced the seven-warning inventory but supplied null database provenance; the policy correctly rejected it as `invalid advisory database`. No validation was relaxed. A normal `cargo audit --file apps/desktop/src-tauri/Cargo.lock --json --deny warnings` then produced acceptable database provenance, scanner exit 1 and the same six maintenance warnings plus the GLib unsoundness warning. Before renewal, the only policy error was the stale GLib fingerprint. Fresh-audit receipt: `6be6855f-2f04-49f7-a3ec-db6a48f4500f`. Raw reports, stderr and preserved scanner exits are local under `.cache/p1-security/commit-review-20260915-qbkY2a/`; they are not committed or represented as zero-advisory passes.

DEDUCED/decision: the fresh target graphs, renewed locked-source construction check, reviewed test-only feature delta and unchanged generation/lock configuration support retaining the existing bounded function deferral. Renew only `reviewContext.sha256` after saving all final inputs. The final inventory also orders the original raw-runner exclusions after the Markdown allowlist so generated Playwright error contexts are not published; that scoping correction reopens the whole-inventory fingerprint, not the unchanged source-construction review. Preserve the advisory identity, rationale, two targets, owner, expiry and six maintenance deferrals; the ten previously documented upstream-withdrawn maintenance exceptions remain retired. Run the unchanged policy on the fresh report after renewal and use that execution result for pass/fail, not this note.

Limits: the 14 generated-source and unstripped Linux ELF receipts above remain historical; no new Linux ELF, current external generated output or unmaterialized macro expansion was inspected. No live browser/audio capture, release packaging, hosted CI or full-history secret scan was performed by this review. This is not library-wide safety, a resolved advisory, a new acceptance claim for the speed feature, or Roadmap completion. Reopen on subsequent lock, feature, target, consumer, generation or toolchain changes.
