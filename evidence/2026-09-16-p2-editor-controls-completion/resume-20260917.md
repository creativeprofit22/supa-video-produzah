# Resumption after interrupted acknowledgement investigation

## Diagnostic cleanup and verification

- HEAD: `809e1102963370c6461f8ad50f7ce28f7f0209bd`, existing dirty tree preserved.
- Removed only the seven temporary `ack_debug_*`/`ACK_DEBUG` additions from `video/project/service.rs`. Its diff against HEAD is now empty (execution `0d1b7a30-8bd5-49ad-b8f9-0f76aba66391`, exit 0). No production performance fix is claimed.
- Preserved the earlier failed and passing results. The interrupted stage logs `ack-stages-20260917-0257.log` and `ack-isolated-stages-20260917-0306.log` yielded no stage measurements on inspection; they do not support a bottleneck attribution.
- Current normal-parallel `cargo test --locked --all-features --manifest-path apps/desktop/src-tauri/Cargo.toml`: execution `ccbd24e4-cc82-4cba-b389-13b18c2586ee`, exit 101. **358 passed, 1 failed, 20 ignored**. The sole failure was `durable_command_acknowledgement_p95_meets_budget`: **628.0756 ms** versus the unchanged **300 ms** budget. Integration tests were not reached. Actual speed, silent-onset, multilayer and gain/fade media tests passed within this failed suite. No serial acceptance substitution or retries to green.
- The above invocation first successfully recorded `stamp-resume-native-before.json`; `stamp-resume-native-after.json` was recorded afterward. Foreground logs are under `C:/Users/SPARTAN PC/.gg/foreground/<execution-id>.log`. These manifests identify source snapshots, not whole-project acceptance.
- Human-mode helper unit tests: execution `5028f451-4d18-48bc-a884-e9fe0b5bcf6b`, exit 0, **9 passed** with `SUPA_VIDEO_TEST_PORT=4183`. This is offline helper verification, not human evidence.
- Cargo formatting: execution `840b0220-34a6-4445-87b6-1cfde0c90f73`, exit 0.

## Audio restoration

At the user's explicit request, Handy PID 3824 and GG Coder's verified WebView audio process PID 16400 were unmuted. The original default-output-only helper found no matching sessions; read-only enumeration located them on another active render endpoint. Executable, process creation time and GG Coder ancestry were checked before restoration. Execution `b723a89d-9a05-4fbc-9c82-cc1701cd9e3b` confirmed both `beforeMuted=True`, `afterMuted=False`, then independently enumerated both as unmuted. No microphone, volume-level, other-session or recording operation was performed. Future capture must reestablish isolation; this is not capture authorization or calibration evidence.

## Remaining gates

The normal native suite remains failed on acknowledgement performance. Live synchronized calibration remains inconclusive; no timing interpretation/tolerance has changed. Native human keyboard interaction is still unexecuted at this checkpoint. Existing native persistence/playback successes remain distinct from these missing gates.

Roadmap remains In progress. Read-only inspection returned revision 186. This resumed session is unbound; supported acquire attempts with null and the approved plan file ID returned `plan-mismatch`. No takeover, Notes write, lifecycle change or Done claim was made.
