# Step 3 continuation — 16 September 2026

The user separately authorized replacing the invalid forced-colors contrast measurement with rendered-color measurement at unchanged thresholds and a deliberately failing contrast control, keeping other accessibility checks and earlier failures.

`forced-colors-contrast.ts` reads Chromium's forced computed `color` (or actual fill for opted-out elements), composites solid ancestor backgrounds, uses Canvas as the root backdrop, and applies 4.5:1 normal / 3:1 large-text thresholds. Unsupported opacity/filter/image/blend compositing fails closed. Disabled controls are exempt from text-contrast requirements. This is computed rendered-color evidence backed by screenshot inspection, not a general screenshot-pixel or native AT verifier. Only forced-mode axe `color-contrast` is replaced; all other axe rules and ordinary-mode contrast stay active. The synthetic #eee-on-white, opted-out 16px control must fail the same assertion.

- Initial authorized replacement run: `f9cdcd36-96b0-4e8a-8d35-6ce6e5448248`, exit 0, three keyboard tests pass.
- Expanded run: `e701f8eb-5113-47bb-bbe0-66bc6cc81bad`, exit 1, 12/13 pass. Newly observed speed fixture race: the 250ms saving timeout can finish before the disabled-state assertion gets scheduled under six-worker load. Existing neighboring `ClipInspector.spec.ts` already uses Playwright's controlled clock for this exact fixture. Speed test now pauses that clock, asserts disabled, advances exactly the existing 250ms and asserts enabled/focused; no duration budget or production behavior changes.
- Cropped screenshot inspection found a genuine audio/source-range reflow defect obscured by page-level overflow clipping. Added panel-local scroll-width assertions. Red reproduction `5aa3bd6d-40f9-4b88-8438-e96c466d30d4`, exit 1: audio panel clips its contents at 320px/200% text.
- Fix: add specific classes to single audio/source panels and share the existing multiselect fieldset/label/input/button/legend sizing and wrapping rules. Transform/opacity styles and behavior unchanged. No redesign.
- Green run `5cb94a4d-a5c4-4446-80e8-4617faad3b43`, exit 0, 13 pass.
- After formatting/rereading the spec and adding cropped multiselect proof: `bf950447-4570-4df3-85ce-6997ea97693f`, exit 0, **13 pass**, normal six-worker configuration. Command: `pnpm --filter @supa-video/desktop test:browser browser-tests/VideoWorkspace.spec.ts browser-tests/WorkspaceLayout.spec.ts browser-tests/ClipSpeedControl.spec.ts --output=../../test-results/editor-controls-step3-final`.

Inspected captures: `audio-text-200.png`, `source-text-200.png`, `layout-320-text200.png`, `bulk-text-200.png` and populated full-page workspace. Cropped single controls now wrap fully with no clipped fields, labels or buttons; source input has visible keyboard focus. Bulk labels and actions remain readable with vertical scrolling. Prior forced-colors diagnostic and desktop capture were also inspected; full-page downscaling is not used as sole proof of local reflow.

Step 3's browser automation and rendered inspection are complete. Human checks and actual Windows/native assistive-technology evidence remain separate, unexecuted step 9 scope, not mislabeled automated evidence. This is not full accessibility certification or whole-phase acceptance. Earlier red runs remain preserved; former source hashes are superseded where these scoped edits changed files.
