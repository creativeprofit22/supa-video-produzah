# P2 editor-controls completion acceptance

## Current closeout — 18 September 2026

**Acceptance incomplete; overall implementation completion not established.** Work is stopped at the user's request, not passed. See [closeout and outstanding checklist](closeout-20260918.md) for separately classified verified passes, failures, inconclusive results and unperformed checks, and [the reconciled matrix](acceptance-matrix.md).

Later evidence verifies native playback/export, automated native keyboard interaction, the slow-speed export repair, and removal of both generations of durable-ack debug probes. It does not verify human checks, complete live timing parity or the durable-ack performance repair. Latest completed browser suite: **91 passed / 11 failed**; latest recorded failing full native suite: **358 passed / 1 failed / 20 ignored**, acknowledgement p95 **628.0756 ms / 300 ms**. The subsequent instrumented native run was interrupted, not passed. Latest capture attempt failed before normal reader-stop acknowledgement; temporary muting was restored. No phase completion or requirement waiver.

The sections below are retained chronological checkpoints, not current instructions to resume work or claims of final acceptance.

## Historical checkpoint — resumed calibration and native evidence

**Acceptance remains incomplete; implementation continues.** Steps 6–7 now have a reviewed, actual bounded digital calibration after fixing a timestamp-field protocol mismatch through offline replay, without changing samples, uncertainty or tolerance. See [calibration review](step-7-calibration-review.md). This does not establish production preview/final timing; step 8 remains open.

Actual native editor edit/reopen/process-restart persistence checks later passed at revision 16, with exact saved state and history verified. See [native recovery evidence](step-9-native-recovery.md). Native playback/end behavior, changed-flow timing and the bounded human keyboard check are still open; step 9 is not wholly complete. Latest recorded ordinary browser suite is **90 passed / 12 failed**, superseding the earlier count below. Subsequent capture-harness edits require fresh affected checks. No Roadmap Done claim or takeover of another session.

## Earlier checkpoint — retained history

**Acceptance remains incomplete.** Steps 1–5 completed; user authorized deferring blocked capture/calibration steps 6–8 and advancing independent work. Native step 9 attempted but did not reach the edit/reopen/restart checks. Steps 10–11 command execution and reporting are complete, not passing acceptance. Read [final verification and criterion map](final-verification.md) first: workspace check/test/build/lint, 355 native unit + 5 integration tests and Clippy pass; normal browser **91 pass / 11 fail**; full format checks fail on pre-existing files. See [step 3 continuation](step-3-continuation.md), [step 4 media and mute fix](step-4-media.md), [step 5 timing diagnosis](step-5-timing-diagnosis.md), [capture access boundary](step-6-capture-boundary.md), and [native partial attempt](step-9-native-partial.md). Earlier sections below preserve prior checkpoint history. No calibrated timing, completed native acceptance, or phase Done claim.

## Baseline — step 1

16 September 2026. HEAD `809e1102963370c6461f8ad50f7ce28f7f0209bd`.
Baseline inspection executions: `b877d983-14bd-46ae-83a7-4cbe29a0352f` and `8e714fce-97e4-43b3-a402-099e5372681a`, both exit 0. Logs are in the local GG foreground directory under those IDs.

Preserved pre-existing dirty files, SHA-256:

| File                                                            | Hash                                                               |
| --------------------------------------------------------------- | ------------------------------------------------------------------ |
| `apps/desktop/src/video/ProgramMonitor.tsx`                     | `9a1a006a741ea0b4f1bff155490b465090b0021baaddedb215d607c9a338cf63` |
| `apps/desktop/src/video/ProgramMonitor.test.tsx`                | `6fa7d0130b9cfd21f66dd8461fced17562b76a4828a4efeaecfdb2f7369ad811` |
| `apps/desktop/browser-tests/ProgramMonitorSharedParity.spec.ts` | `99a6132294d70c1f6e2fbbbf8fbe8ab8af830ede1fae45742db5e71f422011b5` |
| `evidence/2026-09-15-playback-diagnosis.md`                     | `b5f016bf602eb29464718bf31b255daa464fdaafdd5783fdfc36626273b2d8c7` |

The diffs match the diagnosis: exclusive final-media end bound, two rational-rate last-frame regressions, wait for actual paused state, and retain Web Audio latency without subtracting it. No preserved file changed in step 1.

Roadmap initially revision 180. V2 acquire rejected `plan-mismatch` both with the approved file ID and without a plan ID. Supported `bind-current` succeeded at revision 181 for this session; no forced takeover or Notes-file write was used. Final status remains In progress.

Historical evidence is not current execution: September 15 diagnosis records ordinary workspace check/lint/test exit 0 (335 desktop tests), superseding the older claim that the latest ordinary suite necessarily failed. It also preserves shared parity **10 pass / 14 fail**, with unchanged one-frame timing tolerance. Added audio observation reroutes output; raw-media failure is not proof of production timing failure or success.

## Whole-control matrix and closeout

See [acceptance-matrix.md](acceptance-matrix.md) and [closeout-20260918.md](closeout-20260918.md) for current dispositions. [Step 8](step-8-production-timing.md) retains the first qualified timing case and original export failure; [the smooth-transients result](smooth-transients-result.md) records the later scoped repair and passing media checks. The following original inventory is retained as historical context, not the current result table.

## Baseline whole-control inventory — retained

`Located` means source/test inventory, not a new pass. `Historical` means a recorded earlier execution. `Open` requires further inspection/execution. Exact test names below identify assertions rather than inferring every cell from a parent suite pass.

| Control                                             | Draft / commit                                                                                                                    | Exact revision                                                                                                      | Undo / redo                                                                                                    | Reopen / journal recovery                                                                                                             | Locked / stale / pending rejection                                                                                                 | Keyboard / ARIA                                                                                      | 320px / 200% text                                           | Relevant preview / export                                                                          |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| Speed                                               | Located: workspace `keeps speed drafts out of preview, routes Apply, and discards them on revision and selection changes`         | Located: controller `applies speed once through the canonical revision and rejects inexact drafts without dispatch` | Located: native `speed_edits_preserve_positions_and_restore_exact_hashes_through_history`                      | Located: `speed_save_close_and_journal_reopen_preserve_history_hashes_and_exact_retry`, checkpoint-failure tests                      | Located: `service_rejects_locked_overlap_invalid_inexact_stale_and_failed_groups_atomically`; inspect UI pending checks            | Open rendered name/Apply/Reset/focus mapping                                                         | Historical populated workspace browser test; rerun required | Historical eight production exports; decoded seeks pass; calibrated live timing OPEN               |
| Clip gain                                           | Located: multiselect `shows mixed gain rather than the primary and applies once preserving unchanged values`; inspect single gain | Open exact single/bulk assertion mapping                                                                            | Located: `audio_fades_journal_retry_undo_reset_preserve_exact_representation` includes gain                    | Same native test; bulk real-storage coverage OPEN                                                                                     | Located: `blocks the entire selection when one target is locked`; native bulk rollback OPEN                                        | Open rendered mapping                                                                                | Historical populated workspace test; rerun required         | Historical native gain PCM and real browser effects; changed-flow parity OPEN                      |
| Audio fade-in/out                                   | Located: audio native validation; inspect draft controls                                                                          | Open exact single/bulk assertion mapping                                                                            | Located: `audio_fades_journal_retry_undo_reset_preserve_exact_representation`                                  | Same native test; bulk OPEN                                                                                                           | Located: `audio_fades_reject_public_inverse_and_invalid_envelopes_without_mutation`; bulk locked group OPEN                        | Open rendered mapping                                                                                | Historical populated workspace test; rerun required         | Historical linear-amplitude output-frame PCM assertions; rerun required                            |
| Selected source range                               | Located: `routes selected source Apply through canonical trim without legacy drafts or implicit movement`                         | Located: controller `submits trim, move, and caption correction atomically and undoes them together`                | Located: `retimed_split_trim_move_and_ripple_use_exact_sequence_frames_and_restore_hashes`; storage scope OPEN | Open real-storage source trim proof                                                                                                   | Located: `rejects selected source bounds, locked tracks and fade overflow before submitting a group`; stale/pending mapping OPEN   | Open rendered mapping                                                                                | Historical populated workspace test; rerun required         | Nonzero source-in seek/export coverage to map; source audition 1x OPEN                             |
| Multiselect / bulk speed, gain, fades, move, delete | Located: populated browser `populated controller controls commit once, discard stale drafts, bulk history and locked selection`   | Open native exact one-group revision proof per operation                                                            | Browser history is not native inverse proof; native mapping OPEN                                               | Open real-storage bulk proof per operation                                                                                            | Located mixed-selection UI rejection; native zero-partial mutation OPEN; retain target/command bounds and managed/nested rejection | Open timeline keyboard and bulk focus mapping                                                        | Historical populated workspace test; rerun required         | Reuses canonical media paths; independent mute/hidden, eligible multilayer and switching/gaps OPEN |
| Resizable workspace layout                          | Local view only; no project commit                                                                                                | N/A: intentionally no project revision                                                                              | N/A: reset local preference, not project undo                                                                  | Located: `restores and resets only the local layout without replacing children or invoking controller actions`; local preference only | N/A track lock; validate preference bounds                                                                                         | Located browser `LAYOUT-B01 keyboard persistence and narrow/text reflow`; separator/RTL mapping OPEN | Same browser test; rerun/screenshots OPEN                   | N/A media output; preserve child identity                                                          |

Selection is ephemeral and is not claimed to survive project history/recovery. Layout preferences are local only. Transform/opacity are excluded except demonstrated regressions.

## Execution ledger

Step 1 is baseline/inventory documentation only. Step 2 results follow; steps 3–11 remain pending. No audio recording, foreign-session mutation, installation, commit, publication or user-project edit is authorized by this evidence record.

### Step 2 — real native storage and controller coverage

RUNTIME: seven new regressions in `editor_controls_persistence_tests.rs` pass. Each `bulk_{speed,gain,fades,move,delete}_real_storage_history_and_recovery` and `selected_source_range_real_storage_history_and_recovery` proves independent expected state, revision 0→1, exactly one durable command group with matching forward/inverse count, unchanged pre-recovery checkpoint, journal-only replay, exact retry, undo at revision 2, saved/reopened redo at revision 3, exact state hashes and exact restored history group. Bulk edits target two different tracks; source range remains a single-target UI operation. `mixed_locked_bulk_groups_leave_no_partial_state_history_or_journal` covers all six operations, checking live inspector, snapshot and journal bytes, no accepted retry record, and reopened revision/history. The second bulk target is locked, so the first valid edit cannot leak.

- Initial run `1311ee90-797c-4980-aa5d-76442bf1e217`: exit 101, **1 pass / 6 fail**. Test-harness defect, not product regression: calling `recover()` before `open()` checkpoints replayed records (`recovery.rs:262–263`), so the subsequent open correctly reports Clean. Corrected observation order: open performs recovery, then read the checkpoint; kept the Recovered and one-replayed-record assertions unchanged.
- Corrected run `87e47878-535e-4dbb-a88f-564fd9ed1396`: exit 0, **7 pass / 367 filtered / 0 ignored**. Command: `cargo test --locked --all-features --manifest-path apps/desktop/src-tauri/Cargo.toml --lib editor_controls_persistence_tests -- --nocapture`.
- Existing native coverage `2e545695-f90e-4ef2-b526-46e7fa05f32d`: exit 0, **20 pass / 354 filtered / 0 ignored**. Selected audio, speed edit/persistence/timing modules plus `trim_clip_caption_lifecycle_is_atomic_across_history_and_recovery`, `locked_tracks_reject_every_clip_and_caption_mutation`, and `locked_track_does_not_block_mutations_on_an_unlocked_track_and_groups_stay_atomic`.
- Controller/component run `845a0baf-0a76-4022-abac-301b73425cdb`: exit 0, **93 pass, eight files**, normal configured parallelism. Selected `ClipSpeedControl`, `ClipSourceRangeInspector`, `MultiClipInspector`, `clip-selection`, `VideoWorkspace`, `TwoPaneWorkspace`, `test-video-service`, and `use-video-project` tests.

These runs update the matrix's located draft/revision/history/storage/locked checks to current runtime evidence only where their named assertions apply. Mock service history remains UI/planner evidence, not native inverse proof. Specific source-range checks cover draft Reset without commit, duplicate Apply prevention, revision/selection draft discard and locked/saving disabled state. Speed draft tests cover deliberate Apply, reset, exact-duration rejection and revision adoption. Layout tests cover coalesced keyboard drafts, bounds, completion-only local save and child identity. No renderer/API behavior was changed.

CODE: `bulk-clip-edit.ts` retains 1–100 targets, duplicates/stale checks, locked-track rejection, direct-asset/non-nested context and managed-caption rejection, final-state overlap validation. Native existing tests currently pass nested/audio speed rejection and caption-lineage rejection. Existing browser populated-workspace history is later than older multiselect handoff gaps; it is mapped for step 3, not relabeled as missing implementation. This is bounded coverage, not exhaustive native-test execution or final acceptance.

All execution logs above are retained at `C:\Users\SPARTAN PC\.gg\foreground\<execution-id>.log`. New Rust test file was formatted in isolation and reread before the passing execution; no unrelated formatting repairs.

### Step 3 — partial rendered interaction proof; not complete

Design read: existing dense desktop editing workspace, synthetic long asset names, real controller/components with mock IPC; retain existing components, tokens and interaction order. No redesign or production CSS change. Browser automation is not a human or native assistive-technology pass.

Added `completion keyboard audio/source names, Reset, errors and focus` for desktop, true doubled computed text at 320px, and forced-colors/RTL/reduced-motion. After synthetic fixture initialization, sequential Tab/Shift+Tab and typed input exercise volume/fades/source range; invalid values reject Apply, Reset stays draft-only, Apply adopts exactly one revision, focus returns to the input, and controls remain in viewport. Existing populated multiselect tests cover keyboard selection, bulk Apply focus, delete→Undo focus, history and locked selection. Existing layout and speed flows were rerun. Capture destinations now use this completion directory instead of overwriting September 14 evidence.

Commands/results:

- `pnpm --filter @supa-video/desktop test:browser browser-tests/VideoWorkspace.spec.ts browser-tests/WorkspaceLayout.spec.ts browser-tests/ClipSpeedControl.spec.ts --output=../../test-results/editor-controls-completion-step3`: execution `e91f944a-ab28-49b3-8962-9c0c72eec6cf`, exit 1, **12 pass / 1 fail**, 33.4s test time. All interaction assertions completed; forced-colors whole-editing-region axe color contrast is red.
- Formatted only the edited workspace spec, reread, and repeated with output directory `editor-controls-completion-step3-formatted`: execution `5fefa5ba-010a-4a57-b4e4-dd9d648ee42f`, exit 1, **12 pass / 1 fail**, 33.0s. Both traces/error contexts remain in their distinct ignored output directories. No assertion disabled, tolerance changed or dependency upgraded.
- `pnpm --filter @supa-video/desktop check`: `6cfba185-d368-4a34-bcfa-310ba54e3df3`, exit 0 after final spec formatting.

RUNTIME: agent visually inspected `controls-desktop.png`, `controls-text-200.png` and `forced-colors-diagnostic.png`. Full-page narrow capture is very tall and downscaled by the viewer; detailed cropped inspection and remaining layout/multiselect screenshots are still required. Native rendered proof and human/assistive-technology checks have not run.

#### Forced-colors measurement diagnosis (retain red)

Ranked hypotheses were analysis-page loss of media emulation, authored-versus-painted color mismatch, and an actual element opting out of forced colors. Minimal reproduction restricted axe to `#export-title` without editing anything. `453b613d-ffcb-4e39-a9c7-96a589cda299` reproduced the same contrast report with page-scoped and context-scoped forced colors; hypothesis 1 is falsified. `c0c49046-a038-43d2-9f8f-35c16a0a868e` observed computed `color: rgb(0,0,0)`, `-webkit-text-fill-color: rgb(245,245,241)`, forced-color-adjust `auto`. The capture visibly paints black text on white. Installed axe 4.12.1 `axe.js:24947–24948` prefers `-webkit-text-fill-color` over `color`; its reported foreground `#f5f5f1` is exactly the unforced fill value. This establishes an observer mismatch for the export-title finding, not a certificate for all controls or every reported node. No production styling change is justified by that finding alone.

Diagnostic failures retained: `01f92e18-91c5-4554-ac01-1ff270dd1e61` captured computed color but its screenshot locator used region instead of the actual labeled container and timed out. `e8d10427-3753-4b96-becf-6e324348a779` successfully captured the container, then axe rejected its one-page convenience context. The subsequent two-context diagnostic corrected only harness setup. The temporary loopback-only Vite process was explicitly stopped through managed process-tree cleanup.

Next within step 3: finish rendered screenshot inspection and establish a trustworthy forced-color contrast check for the newly added test without suppressing failures or changing production styles just for the scanner. The new forced-colors test intentionally remains red; step 3 is not marked done. Steps 4–11 have not started, preserving approved order. No new live A/V timing result, actual native UI/storage interaction, audio recording, final whole-workspace acceptance, or phase completion is claimed.

Checkpoint diff/hash review `db82968a-3422-4405-abf9-464f3d1fc2d8` exited 0 (`git diff --check`); all four preserved baseline hashes still match. New test-file SHA-256: native persistence `115d16277e68e2ee410de4bcaa11af83a87f6b07e6c2b76c65c9f36b9919a6b5`; project module registration `6f45f45a18a5b8fd74d8f1ca9451b6bade11d34c34eaeeef6a69b03e86739d5e`; workspace browser spec `2ca264b125b05e2bd28fae846b9dc0bffdd7b07fed48de0faeebd1e65d7441d7`; speed browser spec `f3f2a8ccadc39f78de4951d9dd78e0a79cff18a5e854373e40f30f16521749f2`; layout browser spec `9fe235b335cc5606fe987f6ea41e5c11348299d05e0520c2c06941990147c8ce`. This is a partial-progress checkpoint, not plan step 11. Roadmap progress update accepted at revision 182, status remains In progress.
