# Step 6 — capture access boundary (not completed)

Inspected existing `WgcRoi.cpp`, `Loopback.cs`, `owned-browser.mjs`, `capture-lifecycle.mjs`, `roi-observations.mjs`, `bounded-calibration.mjs` and read-only `SessionPreflight.cs`. Did not execute the old calibration path or guardian.

CODE: WGC supports exact tagged HWND/PID ownership predicates, a maximum 16x16 CPU ROI, monotonic compositor timestamps, readiness after first sample, normal stop acknowledgement and a strict six-second cap. The browser launcher uses an isolated temporary profile and owned process identity/job cleanup. ROI helper requires fresh post-readback black/white observations. Existing lifecycle helper is coupled to a guardian that mutates unrelated audio sessions; it is not an authorized completion capture path. Existing Loopback.cs captures the default eRender/eConsole endpoint, not a single process. Its optional legacy visual path is not selected or authorized; completion design must use WGC only.

RUNTIME: freshly compiled/read-only preflight from inspected source, not assumed binary provenance:

`powershell.exe -NoProfile -Mta -Command '$ErrorActionPreference="Stop"; [Environment]::OSVersion.Version.ToString(); Add-Type -Path "evidence/2026-09-14-p2-speed/output-capture/SessionPreflight.cs"; exit [SessionPreflight]::Main(@("--dry-run"))'`

Execution `5047856e-454f-4bb3-bba3-87010d06ee03`, exit 0. Windows **10.0.19045.0**. Default render endpoint has four sessions, one active, three inactive, one system session, one multi-process result; **mutationCalls=0, isolationGuaranteed=false**. Session IDs, endpoint IDs and process identities deliberately not emitted. No samples were recorded.

The locally retained upstream ApplicationLoopback sample requires build 20348+; do not assume a newer process-only route on this host. Tool catalog search exposed no additional isolated audio capture capability. Installing capture drivers/packages or upgrading Windows is not authorized. The existing endpoint recorder has no established exclusive test-only route, and endpoint recording needs separate permission.

Required decision/action: provide a verified test-only audio environment and authorize bounded endpoint recording (at most six seconds per control, no microphone, no desktop-wide video, no uploads, no foreign-session mutation). Alternatively authorize deferring the timing branch and continuing independent acceptance out of the strict original step order. Neither missing proof alone nor a recoverable tool failure is treated as the external blocker: the blocker is current unisolated endpoint plus absent recording permission.

The user subsequently authorized bounded endpoint recording **only after test-only isolation is verified**, and stated other sound apps/notifications were closed/silenced. Recheck `ce8b4c6d-f9b4-46a3-8dfd-f953282ee8dc` still reported four sessions, one active, isolation false. Added and compiled a minimal read-only `IsolationReadiness.cs` against the inspected COM interfaces to identify the outstanding action without reading audio, paths or command lines. Execution `31216102-66ea-432a-8e8c-2e32d72b70d5` exited 2 (intentional fail-closed): three non-expired unmuted foreign sessions, including **active Discord**, inactive Discord and inactive Microsoft Edge WebView2. No owned test player was running. Capture permission now exists conditionally, but its isolation condition is not met. User must quit Discord and silence the remaining WebView audio session, or provide an isolated output route; the agent must not mutate those sessions. No recording took place. After the user reported fully quitting Discord and muting WebView2, execution `87129d2a-477a-436e-b295-636edd556be6` still returned the same three unmuted sessions and intentional exit 2. The tool-observed isolation condition remains unmet despite the reported manual changes; no repeated capture attempts or automatic session changes are justified.

No new recorder was run or claimed qualified. Steps 7–8 cannot execute before this access boundary and calibration. Step 9 native UI and step 10 independent final commands remain unexecuted under strict ordering.

Checkpoint checks: affected ESLint and `git diff --check` passed in `3c4c8615-6d1c-4f3b-aef1-7f965dd8166d`; source/test type checks and affected tests are recorded in step 4. Curated evidence execution IDs were checked against retained local logs in `b67f3b5b-e8e4-4b00-833d-af9f5d91b8d9`; all existed. Initial shell-quoting failure in that bookkeeping check (`2a84814e-c6ad-4948-8ffb-985eb408535e`) performed no mutation and was corrected.
